{
  "slug": "mongodb-atlas",
  "name": "MongoDB Atlas",
  "description": "Monitor MongoDB Atlas organization user access, team role assignments, infrastructure inventory, and encryption at rest configuration",
  "category": "Cloud",
  "docsUrl": "https://www.mongodb.com/docs/atlas/configure-api-access/",
  "baseUrl": "https://cloud.mongodb.com/api/atlas/v2/",
  "authConfig": {
    "type": "custom",
    "config": {
      "setupInstructions": "1. In MongoDB Atlas, go to Organization > Access Manager > Applications.\n2. Click 'Add Service Account', name it (e.g. 'Comp AI'), choose a secret expiry, and grant it the 'Organization Read Only' role.\n3. Click Create, then copy the Client ID and the Client Secret (the secret is shown only once).\n4. Find your Organization ID under Organization Settings (or the cloud.mongodb.com URL after /org/).\n5. Enter the Client ID, Client Secret, and Organization ID below.",
      "credentialFields": [
        {
          "label": "Service Account Client ID",
          "type": "text",
          "required": true,
          "helpText": "From Organization > Access Manager > Applications > your Service Account. Looks like mdb_sa_id_..."
        },
        {
          "label": "Service Account Client Secret",
          "type": "password",
          "required": true,
          "helpText": "Shown only once when the Service Account is created. Looks like mdb_sa_sk_..."
        },
        {
          "label": "Organization ID",
          "type": "text",
          "required": true,
          "helpText": "Your MongoDB Atlas Organization ID (Organization Settings, or the 24-character hex in the cloud.mongodb.com URL)."
        }
      ]
    }
  },
  "capabilities": [
    "checks"
  ],
  "supportsMultipleConnections": false,
  "syncSupported": false,
  "checks": [
    {
      "slug": "employee_access",
      "name": "Employee Access",
      "description": "Lists all users in the MongoDB Atlas organization with their roles and last authentication time",
      "defaultSeverity": "medium",
      "enabled": true
    },
    {
      "slug": "role_based_access",
      "name": "Role-based Access Controls",
      "description": "Lists all teams in the MongoDB Atlas organization with their members and role assignments",
      "defaultSeverity": "medium",
      "enabled": true
    },
    {
      "slug": "infrastructure_inventory",
      "name": "Infrastructure Inventory",
      "description": "Lists all projects and clusters in the MongoDB Atlas organization",
      "defaultSeverity": "medium",
      "enabled": true
    },
    {
      "slug": "encryption_at_rest",
      "name": "Encryption at Rest",
      "description": "Verifies encryption at rest configuration for each project in the MongoDB Atlas organization",
      "defaultSeverity": "medium",
      "enabled": true
    },
    {
      "slug": "mongodb_separation_of_environments",
      "name": "Separation of Environments",
      "description": "Verifies that MongoDB Atlas organization has separate projects for different environments (e.g., development, staging, production), ensuring proper environment segregation.",
      "defaultSeverity": "high",
      "enabled": true
    }
  ],
  "checkCount": 5,
  "isActive": true
}
